spring:
  profiles:
    active: local
  datasource:
    username: dsgw
  jackson:
    default-property-inclusion: non_absent

server:
  servlet:
    context-path: /worker

logging:
  level:
    me.kolek: DEBUG