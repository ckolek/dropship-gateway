type PageInfo {
    count: Int!
    totalCount: Int!
    pageNumber: Int!
    totalPages: Int!
}

interface Connection {
    edges: [Edge!]!
    pageInfo: PageInfo!
}

interface Edge {
    cursor: ID!
}